#!/usr/bin/python3

def add_variables(a=0, b=0) -> int:
  """
  This function adds two integers
  without adding them directly
  """
  while b >= 0:
    a += 1
    b -= 1
   return a
add_varables(3,4)
## 7
